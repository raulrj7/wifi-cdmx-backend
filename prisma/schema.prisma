generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model WifiPoint {
  id        Int      @id @default(autoincrement())
  wifi_id   String?  // Opcional
  program   String?  // Opcional
  latitude  Float?   // Opcional
  longitude Float?   // Opcional
  district  String?  // Opcional
}
